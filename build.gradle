apply plugin: 'java'

sourceSets{
	api
	main{
		compileClasspath = compileClasspath + files(api.output.classesDir)
	}
	
	classes.dependsOn 'createGradleWrapper',apiClasses
	
	task createGradleWrapper(type: Wrapper){
		gradleVersion = '1.3'
	}
}